# Core runtime + quality gate (NO torch, NO GPU). Reproduces the 45 tests, ruff, mypy,
# the Tier-A scenario harness, and the entire tabular pipeline `python -m inventory_rl.cli all`.
#
# Frozen lock, verified in a clean Python 3.12 venv (no torch installed). gymnasium is pinned
# to 1.2.3 so the stretch path (which adds Stable-Baselines3) resolves against the same version.
# Install:  pip install -r requirements-core.txt  then  pip install -e . --no-deps
ast-serialize==0.5.0
cloudpickle==3.1.2
colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
farama-notifications==0.0.6
fonttools==4.63.0
gymnasium==1.2.3
iniconfig==2.3.0
kiwisolver==1.5.0
librt==0.11.0
matplotlib==3.11.0
mypy==2.1.0
mypy-extensions==1.1.0
numpy==2.4.6
packaging==26.2
pandas==3.0.3
pathspec==1.1.1
pillow==12.2.0
pluggy==1.6.0
pygments==2.20.0
pyparsing==3.3.2
pytest==9.1.0
python-dateutil==2.9.0.post0
ruff==0.15.17
scipy==1.17.1
six==1.17.0
typing-extensions==4.15.0
tzdata==2026.2
