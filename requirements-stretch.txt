# PPO stretch = core + CPU PyTorch + Stable-Baselines3. Only needed to RETRAIN PPO; the
# committed models/ppo_*.zip evaluate without it, and the core path needs none of this.
# Install:  pip install -r requirements-stretch.txt  then  pip install -e . --no-deps
#
# torch here is the default wheel; for a guaranteed CPU build use the official index:
#   pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cpu
-r requirements-core.txt
torch==2.12.0
stable-baselines3==2.8.0
# torch transitive deps, pinned for reproducibility of the stretch path
filelock==3.29.4
fsspec==2026.4.0
jinja2==3.1.6
markupsafe==3.0.3
mpmath==1.3.0
networkx==3.6.1
sympy==1.14.0
